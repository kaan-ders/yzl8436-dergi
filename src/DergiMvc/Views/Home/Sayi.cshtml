@{
}

@model List<SayiViewModel>

<div class="card mt-3">
    <div class="card-header">
        Sayılar
    </div>
    <div class="card-body">
        <table class="table">
            <tr>
                <th>No</th>
                <th>Yayın Tarihi</th>
                <th>#</th>
            </tr>
            @foreach (var sayi in Model)
            {
                <tr>
                    <td>@sayi.No</td>
                    <td>@sayi.YayinTarihi.ToString("dd.MM.yyyy")</td>
                    <td>
                        <a href="/Home/Makaleler/@sayi.Id">makaleler</a>
                    </td>
                </tr>
            }
        </table>
    </div>
</div>